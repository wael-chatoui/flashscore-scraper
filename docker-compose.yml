services:
  scraper:
    build: .
    container_name: flashscore-volleyball-scraper
    env_file: .env
    command: ["sh", "-c", "python -m flashscore_scraper --days=0; python -m flashscore_scraper --days=1"]
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./output:/app/output
    restart: "no"
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  hockey-scraper:
    build: .
    container_name: flashscore-hockey-scraper
    env_file: .env
    command: ["sh", "-c", "python -m flashscore_scraper --sport=hockey --days=0; python -m flashscore_scraper --sport=hockey --days=1"]
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./output:/app/output
    restart: "no"
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  football-scraper:
    build: .
    container_name: flashscore-football-scraper
    env_file: .env
    command: ["sh", "-c", "python -m flashscore_scraper --sport=football --days=0; python -m flashscore_scraper --sport=football --days=1"]
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./output:/app/output
    restart: "no"
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  # Optional: Scheduled runner using ofelia (cron for Docker)
  scheduler:
    image: mcuadros/ofelia:latest
    container_name: flashscore-scheduler
    depends_on:
      - scraper
      - hockey-scraper
      - football-scraper
    command: daemon --docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - .:/root/flashscore-scraper
    labels:
      ofelia.job-local.volleyball.schedule: "0 0 1 * * *"
      ofelia.job-local.volleyball.command: "/root/flashscore-scraper/scripts/run_volleyball_docker.sh"
      ofelia.job-local.hockey.schedule: "0 0 2 * * *"
      ofelia.job-local.hockey.command: "/root/flashscore-scraper/scripts/run_hockey_docker.sh"
      ofelia.job-local.football.schedule: "0 0 3 * * *"
      ofelia.job-local.football.command: "/root/flashscore-scraper/scripts/run_football_docker.sh"
    profiles:
      - scheduled  # Only start with: docker compose --profile scheduled up
